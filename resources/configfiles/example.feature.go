package configfiles

// GetExampleFeature returns a sample feature file that can be ran to ensure the new project is configured correctly
func GetExampleFeature() string {
	return "example feature file"
}
